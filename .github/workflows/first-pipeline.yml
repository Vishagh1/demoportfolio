name: first-pipeline

on:
    push:
        branches:
            - main

jobs:
    first-job:
        runs-on: ubuntu-latest

        steps:
            - name: Code Copy
              uses: actions/checkout@v4

            - name: Build Image
              run: docker build -t dckrimg .
